---
# vars file for setup_vim
bin: "{{ ansible_env.HOME }}/bin"
vim_dir: "{{ ansible_env.HOME }}/.vim"
vimrc: "{{ ansible_env.HOME }}/.vimrc"
install_packages:
  - fonts-powerline
  - git
  - vim
  - vim-nox
